version: 2.1
jobs:
  build:
    docker:
      - image: tiltdev/tilt:latest

    steps:
      - setup_remote_docker
      - checkout
      - run: apt install -y golang git
      - run: with-kind-cluster.sh test/test.sh